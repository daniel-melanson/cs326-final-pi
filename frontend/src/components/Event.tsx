import Enact from "../Enact";
import { formatDateAsTime } from "./util";

interface EventsProps {
    details: APIProfileEvents;
  }

export default function Event(props: EventsProps){
    const { details } = props;
    

}